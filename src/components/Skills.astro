<!-- ---
// Skills section component - modular and reusable
import { skillsData, themeConfig } from '../data/index.js';
import SectionHeader from './ui/SectionHeader.astro';
import SkillItem from './sections/SkillItem.astro';
---

<article id={skillsData.sectionId} class={skillsData.layout.wrapperClass}>
  <div 
    class={skillsData.layout.containerClass}
    data-sal="fade"
    data-sal-duration="800"
  >
    <SectionHeader 
      title={skillsData.title}
      subtitle={skillsData.subtitle}
    />

    {skillsData.categories.map((category, categoryIndex) => (
      <section class="skill-category" style="margin-bottom: 3em;">
        <h3 
          class="category-title"
          data-sal="slide-right" 
          data-sal-delay={200 + (categoryIndex * 100)}
        >
          {category.name}
        </h3>
        
        <div class="row gtr-uniform">
          {category.skills.map((skill, skillIndex) => (
            <div class="col-6 col-12-medium">
              <SkillItem 
                name={skill.name}
                proficiency={skill.proficiency}
                yearsExperience={skill.yearsExperience}
                description={skill.description}
                icon={skill.icon}
                techColor={skill.techColor}
                specialties={skill.specialties}
                data-sal="fade"
                data-sal-delay={300 + (skillIndex * 100)}
              />
            </div>
          ))}
        </div>
      </section>
    ))}
  </div>
</article>

<style>
  @import '../styles/sections/skills.css';
</style> -->